<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chaussuresapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".AddAnnonce"
            android:label="@string/title_activity_addannonce"></activity>
        <activity
            android:name=".DescriptionAnnonce"
            android:label="@string/title_activity_description_annonce" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- PROFILE ACTIVITY -->
        <activity
            android:name=".auth.ProfileActivity"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>